<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Style.Settings.Title" parent="Widget.MaterialComponents.TextView">
        <item name="android:layout_marginStart">@dimen/large_225</item>
        <item name="android:textAppearance">@style/TextAppearance.MaterialComponents.Subtitle1</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="CustomStepperLayoutTheme" parent="MSDefaultStepperLayoutTheme">
        <item name="ms_tabsContainerStyle">@style/CustomStepTabsContainerStyle</item>
        <item name="ms_stepTabTitleStyle">@style/CustomStepTabsTitleStyle</item>

    </style>
    <style name="CustomStepTabsContainerStyle" parent="MSStepTabsContainer">
        <item name="android:background">@color/darkblue</item>
    </style>
    <style name="CustomStepTabsTitleStyle" parent="MSStepTabTitle">
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
